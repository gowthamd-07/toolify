apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: toolify
  namespace: application
spec:
  hosts:
    - "*"
  gateways:
    - toolify
  http:
    - name: toolify
      match:
        - uri:
            exact: /
      route:
      - destination:
          host: toolify
          port:
            number: 8000
          subset: 'd2c5eea'
        weight: 100
      - destination:
          host: toolify
          port:
            number: 8000
          subset: 'bd3364a'
        weight: 0