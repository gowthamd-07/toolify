apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: toolify
  namespace: application
spec:
  hosts:
    - toolify
  http:
  - route:
    - destination:
        host: toolify
        subset: v3